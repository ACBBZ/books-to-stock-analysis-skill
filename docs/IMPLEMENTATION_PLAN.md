# MVP implementation plan

1. Define typed Trading Skill IR and JSON Schema.
2. Implement a whitelist-only rule interpreter.
3. Add deterministic feature primitives.
4. Evaluate prerequisite, required, confirmation, and rejection rules with evidence.
5. Add JSON/YAML adapters and CLI commands.
6. Add a synthetic skill pack, tests, CI, safety documentation, and packaging.

The next implementation cycle adds versioned OHLCV data contracts and a detector registry before any PDF extraction or multi-agent orchestration work.
